[project]
name = "pyproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rtoaiengineer", email = "muhammad.irsyad1617@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.2",
    "torch>=2.13.0",
]

[project.scripts]
pyproject = "pyproject:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
